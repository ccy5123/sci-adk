# sci-adk Milestone 1: Python Base Environment
# Scientific computing stack for T-1 molecular encoding

# Core scientific computing
numpy>=1.24.0
scipy>=1.10.0

# Graph theory (for molecular graphs)
networkx>=3.0

# Chemistry (for molecular structures - optional, milestone 2+)
# RDKit is large, defer to actual need
# rdkit-pypi>=2022.9.0

# Symbolic math (for number theory - optional, milestone 2+)
# sympy>=1.11

# Data validation (Pydantic v2 for compatibility with sci-adk core)
pydantic>=2.0.0

# NOTE: pypdf[crypto] is intentionally NOT here. PDF normalization
# (sci_adk.search.pdf_normalize, driven by loop/literature_acquirer) runs
# host-side during acquisition, not inside this Docker experiment container.
# It is a core dependency of sci-adk itself (pyproject.toml), so the container
# does not need a PDF library.

# Development tools (optional, for engineering layer)
# pytest>=7.0.0
# pytest-cov>=4.0.0
# black>=23.0.0
# ruff>=0.0.270
