# sci-adk Milestone 1: Python Base Environment
# Scientific computing stack for T-1 molecular encoding

# Core scientific computing
numpy>=1.24.0
scipy>=1.10.0

# Graph theory (for molecular graphs)
networkx>=3.0

# Chemistry (for molecular structures).
# Activated for paper-figures Phase 4-2: the adapter's RDKitDockerPlotter draws a
# DETERMINISTIC 2D molecular-structure PNG inside this container so a molecular paper
# can carry a real structure figure (design/paper-figures-and-si.md, O-A resolution).
# Use the MODERN official wheel `rdkit` (NOT the legacy `rdkit-pypi`), PINNED so the
# 2D coordinate layout + Cairo PNG bytes are reproducible across rebuilds (D2: same
# molecule -> byte-identical PNG depends on a fixed RDKit version). cp311 manylinux
# wheel (this image is python:3.11-slim).
rdkit==2024.9.6

# Symbolic math (for number theory - optional, milestone 2+)
# sympy>=1.11

# Data validation (Pydantic v2 for compatibility with sci-adk core)
pydantic>=2.0.0

# NOTE: pypdf[crypto] is intentionally NOT here. PDF normalization
# (sci_adk.search.pdf_normalize, driven by loop/literature_acquirer) runs
# host-side during acquisition, not inside this Docker experiment container.
# It is a core dependency of sci-adk itself (pyproject.toml), so the container
# does not need a PDF library.

# Development tools (optional, for engineering layer)
# pytest>=7.0.0
# pytest-cov>=4.0.0
# black>=23.0.0
# ruff>=0.0.270
