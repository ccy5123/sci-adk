# runs/ — system output

Each subdirectory is one research "compilation": one proposal in, one
`spec.json` + `evidence/` + `claims/` + `code/` + `data/` + `paper/` out.

This is sci-adk's **output**, not its source. See
[design/directory-structure.md](../design/directory-structure.md).
